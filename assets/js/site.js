(function () {
  'use strict';
  var ADMIN_API_BASE = window.APEX_ADMIN_API_BASE || '';
  var data = null;
  var page = document.documentElement.getAttribute('data-page') || '';
  function get(object, path) { return path.split('.').reduce(function (value, key) { return value == null ? undefined : value[key]; }, object); }
  function escapeHtml(value) { return String(value == null ? '' : value).replace(/[&<>"']/g, function (character) { return {'&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;'}[character]; }); }
  function initNav() { var toggle = document.getElementById('navToggle'), links = document.getElementById('navLinks'); if (!toggle || !links) return; toggle.addEventListener('click', function () { var open = links.classList.toggle('is-open'); toggle.setAttribute('aria-expanded', open ? 'true' : 'false'); }); links.querySelectorAll('a').forEach(function (anchor) { anchor.addEventListener('click', function () { links.classList.remove('is-open'); toggle.setAttribute('aria-expanded', 'false'); }); }); links.querySelectorAll('[data-page]').forEach(function (anchor) { if (anchor.getAttribute('data-page') === page) { anchor.classList.add('is-active'); anchor.setAttribute('aria-current', 'page'); } }); }
  function bindValue(root, object) { root.querySelectorAll('[data-bind]').forEach(function (element) { var path = element.getAttribute('data-bind'), value = path === '.' ? object : get(object, path); if (value == null) return; if (element.tagName === 'IMG') element.src = value; else element.textContent = value; }); root.querySelectorAll('[data-class]').forEach(function (element) { element.className = element.getAttribute('data-class').replace(/\{([^}]+)\}/g, function (_, key) { return object[key] || ''; }); }); root.querySelectorAll('[data-href]').forEach(function (element) { var key = element.getAttribute('data-href'), value = key === 'detailUrl' ? object.detailUrl : get(object, key); if (value) element.href = value; }); root.querySelectorAll('[data-copy-bind]').forEach(function (element) { var value = get(object, element.getAttribute('data-copy-bind')); if (value) element.dataset.copy = value; }); }
  function renderRepeats() { document.querySelectorAll('[data-repeat]').forEach(function (container) { var values = get(data, container.getAttribute('data-repeat')) || [], template = container.firstElementChild; if (!template) return; container.innerHTML = ''; values.forEach(function (item) { var node = template.cloneNode(true); if (container.getAttribute('data-repeat') === 'services') item.detailUrl = 'service-' + item.id + '.html'; if (container.getAttribute('data-repeat') === 'projects') item.detailUrl = 'project-' + item.id + '.html'; bindValue(node, item); node.querySelectorAll('[data-repeat]').forEach(function (nested) { var nestedValues = get(item, nested.getAttribute('data-repeat')) || [], nestedTemplate = nested.firstElementChild; nested.innerHTML = ''; nestedValues.forEach(function (value) { var nestedNode = nestedTemplate.cloneNode(true); nestedNode.querySelectorAll('[data-bind]').forEach(function (element) { element.textContent = value; }); nested.appendChild(nestedNode); }); }); container.appendChild(node); }); }); }
  function renderDetail() { var target = document.getElementById('detail-content'); if (!target || !data) return; var match = window.location.pathname.match(/(service|project)-([^/]+)\.html$/); if (!match) return; var type = match[1], item = (type === 'service' ? data.services : data.projects).find(function (entry) { return entry.id === match[2]; }); if (!item) { target.innerHTML = '<h1>Content not found</h1>'; return; } var back = document.querySelector('[data-back-link]'); if (back) back.href = type === 'service' ? 'services.html' : 'projects.html'; if (type === 'service') { target.innerHTML = '<div class="detail-hero"><div><span class="eyebrow"><span class="sq"></span>' + escapeHtml(item.number) + ' — Service</span><h1>' + escapeHtml(item.title) + '</h1><p class="detail-hero lead">' + escapeHtml(item.short) + '</p><div class="detail-meta"><h3>Capabilities</h3><ul class="taglist">' + (item.tags || []).map(function (tag) { return '<li>' + escapeHtml(tag) + '</li>'; }).join('') + '</ul></div></div><figure class="detail-image"><img src="' + escapeHtml(item.image) + '" alt=""><figcaption>' + escapeHtml(item.figureCaption || '') + '</figcaption></figure></div><div class="detail-copy"><p>' + escapeHtml(item.description) + '</p></div>' + (item.stats && item.stats.length ? '<div class="assets"><div class="assets__head"><h4>Reference material on hand</h4></div><div class="stat-grid">' + item.stats.map(function (stat) { return '<div class="stat"><b>' + escapeHtml(stat.value) + '</b><span>' + escapeHtml(stat.label) + '</span></div>'; }).join('') + '</div></div>' : '') + '<div class="detail-meta"><h3>Notes</h3>' + (item.notes || []).map(function (note) { return '<p>' + escapeHtml(note) + '</p>'; }).join('') + '</div>'; } else { target.innerHTML = '<div class="detail-hero"><div><span class="eyebrow"><span class="sq"></span>' + escapeHtml(item.tag) + '</span><h1>' + escapeHtml(item.title) + '</h1><p class="detail-hero lead">' + escapeHtml(item.short) + '</p></div><figure class="detail-image"><img src="' + escapeHtml(item.image) + '" alt="' + escapeHtml(item.title) + '"></figure></div><div class="detail-copy">' + item.fullText.split(/\n\n/).map(function (paragraph) { return '<p>' + escapeHtml(paragraph) + '</p>'; }).join('') + '</div><div class="detail-meta"><h3>Project vision</h3><p>' + escapeHtml(item.vision) + '</p></div>'; } }
  function initForm() { var form = document.getElementById('enquiryForm'), status = document.getElementById('formStatus'); if (!form || !status || !data) return; form.addEventListener('submit', function (event) { event.preventDefault(); var name = form.name.value.trim(), organization = form.org.value.trim(), email = form.email.value.trim(), topic = form.topic.value, message = form.message.value.trim(); if (!name || !email || !topic || !message) { status.textContent = 'Please complete the required fields before continuing.'; return; } if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { status.textContent = 'Please enter a valid email address.'; return; } var subject = 'Enquiry: ' + topic + (organization ? ' — ' + organization : ''), body = 'Name: ' + name + '\nOrganization: ' + (organization || '-') + '\nEmail: ' + email + '\nEnquiry type: ' + topic + '\n\n' + message; window.location.href = 'mailto:' + data.site.email + '?subject=' + encodeURIComponent(subject) + '&body=' + encodeURIComponent(body); status.textContent = 'Opening your email app…'; }); }
  function downloadJson(next) { var blob = new Blob([JSON.stringify(next, null, 2) + '\n'], { type: 'application/json' }), url = URL.createObjectURL(blob), anchor = document.createElement('a'); anchor.href = url; anchor.download = 'data.json'; document.body.appendChild(anchor); anchor.click(); anchor.remove(); URL.revokeObjectURL(url); }
  function openAdmin() { var root = document.getElementById('admin-root'); if (!root || !data) return; var token = sessionStorage.getItem('apexAdminToken') || ''; root.innerHTML = '<div class="admin-backdrop"></div><aside class="admin-panel"><button class="admin-close" aria-label="Close">×</button><h2>Site editor</h2><p class="admin-help">Edit the structured content below. If a Worker is configured, Save to GitHub commits it to the repository. Otherwise, Download data.json gives you a file to upload manually.</p><label>Admin password</label><input id="admin-password" type="password" placeholder="Required for GitHub save"><label>Content JSON</label><textarea id="admin-json" rows="18"></textarea><label>Upload image</label><input id="admin-image" type="file" accept="image/*"><div class="admin-actions"><button class="btn btn--primary" id="admin-login">Login</button><button class="btn btn--dark" id="admin-save">Save to GitHub</button><button class="btn btn--dark" id="admin-download">Download data.json</button></div><div class="admin-status" id="admin-status"></div></aside>'; var panel = root.querySelector('.admin-panel'), jsonField = panel.querySelector('#admin-json'); jsonField.value = JSON.stringify(data, null, 2); function close() { root.innerHTML = ''; } root.querySelector('.admin-backdrop').onclick = close; root.querySelector('.admin-close').onclick = close; panel.querySelector('#admin-login').onclick = function () { var status = panel.querySelector('#admin-status'); if (!ADMIN_API_BASE) { status.textContent = 'No Worker URL is configured. Use Download data.json for local editing, or configure APEX_ADMIN_API_BASE first.'; return; } fetch(ADMIN_API_BASE.replace(/\/$/, '') + '/login', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ password: panel.querySelector('#admin-password').value }) }).then(function (response) { return response.json().then(function (body) { return { ok: response.ok, body: body }; }); }).then(function (result) { if (!result.ok) throw Error(result.body.error || 'Login failed'); token = result.body.token; sessionStorage.setItem('apexAdminToken', token); status.textContent = 'Logged in. You can now save to GitHub.'; }).catch(function (error) { status.textContent = error.message; }); }; panel.querySelector('#admin-download').onclick = function () { try { downloadJson(JSON.parse(jsonField.value)); panel.querySelector('#admin-status').textContent = 'Downloaded data.json. Upload it to the repository root to publish the changes.'; } catch (error) { panel.querySelector('#admin-status').textContent = 'Invalid JSON: ' + error.message; } }; panel.querySelector('#admin-save').onclick = function () { var status = panel.querySelector('#admin-status'); if (!ADMIN_API_BASE) { status.textContent = 'No Worker URL is configured. Use Download data.json instead.'; return; } if (!token) { status.textContent = 'Log in before saving to GitHub.'; return; } var next; try { next = JSON.parse(jsonField.value); } catch (error) { status.textContent = 'Invalid JSON: ' + error.message; return; } var file = panel.querySelector('#admin-image').files[0], send = function (images) { fetch(ADMIN_API_BASE.replace(/\/$/, '') + '/save', { method: 'POST', headers: {'Content-Type':'application/json', 'Authorization':'Bearer ' + token}, body: JSON.stringify({ dataJson: next, images: images, commitMessage: 'Admin: update Apex Bionet content' }) }).then(function (response) { return response.json().then(function (body) { return { ok: response.ok, body: body }; }); }).then(function (result) { if (!result.ok) throw Error(result.body.error || 'Save failed'); data = next; status.textContent = 'Saved to GitHub successfully.'; }).catch(function (error) { status.textContent = error.message; }); }; if (!file) { send([]); return; } var reader = new FileReader(); reader.onload = function () { send([{ path: 'assets/images/admin-' + Date.now() + '-' + file.name.replace(/[^a-z0-9._-]/gi, '-'), base64: reader.result }]); }; reader.readAsDataURL(file); }; }
  function initAdmin() { document.querySelectorAll('.admin-open').forEach(function (button) { button.addEventListener('click', openAdmin); }); }
  initNav(); initAdmin();
  var embedded = document.getElementById('detail-data');
  var promise = embedded ? Promise.resolve(JSON.parse(embedded.textContent)) : fetch('data.json').then(function (response) { if (!response.ok) throw Error('Content file returned ' + response.status); return response.json(); });
  promise.then(function (loaded) { data = loaded; document.querySelectorAll('[data-bind]').forEach(function (element) { var value = get(data, element.getAttribute('data-bind')); if (value !== undefined && typeof value !== 'object') element.textContent = value; }); renderRepeats(); renderDetail(); bindValue(document, data); document.querySelectorAll('[data-href="emailHref"]').forEach(function (element) { element.href = 'mailto:' + data.site.email; }); document.querySelectorAll('[data-href="phoneHref"]').forEach(function (element) { element.href = 'tel:' + data.site.phone.replace(/\s/g, ''); }); document.querySelectorAll('[data-href="webHref"]').forEach(function (element) { element.href = 'https://' + data.site.web; }); initForm(); }).catch(function (error) { console.error(error); var detail = document.getElementById('detail-content'); if (detail) detail.innerHTML = '<h1>Unable to load this page content</h1><p>Please redeploy the complete site package, including data.json.</p>'; });
}());
